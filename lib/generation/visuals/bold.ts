import type { VisualSpec } from "./spec";

/**
 * «bold» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const boldVisual: VisualSpec = {
  id: "bold",
  base: "hero-split",
  plan: {},
};

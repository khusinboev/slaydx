import type { VisualSpec } from "./spec";

/**
 * «circle» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const circleVisual: VisualSpec = {
  id: "circle",
  base: "cards",
  plan: {},
};

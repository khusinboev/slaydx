import type { VisualSpec } from "./spec";

/**
 * «split» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const splitVisual: VisualSpec = {
  id: "split",
  base: "hero-split",
  plan: {},
};

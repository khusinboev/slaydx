import type { VisualSpec } from "./spec";

/**
 * «formal» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const formalVisual: VisualSpec = {
  id: "formal",
  base: "dense",
  plan: {},
};

import type { VisualSpec } from "./spec";

/**
 * «academic» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const academicVisual: VisualSpec = {
  id: "academic",
  base: "classic",
  plan: {},
};

import type { VisualSpec } from "./spec";

/**
 * «notebook» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const notebookVisual: VisualSpec = {
  id: "notebook",
  base: "lab",
  plan: {},
};

import type { VisualSpec } from "./spec";

/**
 * «editorial» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const editorialVisual: VisualSpec = {
  id: "editorial",
  base: "cards",
  plan: {},
};

import type { VisualSpec } from "./spec";

/**
 * «dashboard» dizayni — STUB (Shablonlar 2, A2). Maxsus maketlar hali yo'q:
 * hammasi `base` oilasi bilan chiziladi. Dizayn shu faylda to'ldiriladi.
 */
export const dashboardVisual: VisualSpec = {
  id: "dashboard",
  base: "dense",
  plan: {},
};

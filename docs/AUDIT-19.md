# AUDIT-19 — Talaba ishlari 2: kurs ishi, referat, mustaqil ish, insho, tezis

Davomi: `docs/AUDIT-18.md` (Maqola 3). Boshlangan: 2026-09-15. Reja `~/.claude/plans/sen-senior-fullstack-dev-majestic-starfish.md` (tasdiqlangan 2026-09-15).

## 1. Reja (tasdiqlangan)

### Kontekst

«Talaba ishlari» bo'limidagi 5 vosita (`coursework`, `referat`, `mustaqil-ish`, `essay`, `thesis`) oxirgi 6 sprintda auditdan o'tmagan «eski avlod» yo'lda: bitta umumiy `writeWriterWithLlm` (+`writeEssayWithLlm`, tezis uchun orfan `writeImradWithLlm`), to'g'ridan-to'g'ri Gemini (`llm.ts` — zaxira zanjiri va `cost_json` yo'q), **adabiyotlar model tomonidan o'ylab topiladi** («TEKSHIRILMAGAN» izohi bilan), rasm/sxema yo'q, sifat hisoboti/avto-sayqal yo'q, ko'ruvchida tahrir yo'q (`edit-adapters.ts` da adapter yo'q), janr farqi `prompts.ts:170–176` da 4 ta `if`, DOCX va ko'ruvchi «yagona manba»siz, halollik chegarasi ulanmagan, jonli holat faqat essay+coursework, forma reyestr/zondsiz (`ministry` 4 vositada yashirin, `includeVisuals` maydonsiz `true`). Kurs ishi 24 000 tangagacha sotiladi — bo'limning eng qimmat hujjati eng zaif dvigatelda.

Maqola 2/3 (AUDIT-17/18) ning butun infratuzilmasi tayyor: `research/` (OpenAlex+Crossref, `verifyCitations`), `figures/` (10 sxema turi), `cite/` (GOST/APA/IEEE), `review.ts` (qoidalar + Claude baholovchi), `polish.ts` (avto-sayqal, Q-2 halollik, Q-3 faqat ball oshsa), `guard.ts`, `llm-roles.ts` (`complete`, `CostMeter`), `layout.ts planArticle` (yagona manba), `types-registry` (skelet+`guidance`+`judge`), `article-params` (differensial zond). Maqsad — shu darajani beshala vositaga chiqarish, har janrning O'zbekiston OTM standartlariga (internet tadqiqoti: BuxDU/TATU/TSUULL/TDIU uslubiy ko'rsatmalari, VM 824, ГОСТ 7.32-2017, DTM/IELTS mezonlari) qat'iy amal qilgan holda.

### Mahsulot egasi qarorlari (qayta so'ralmaydi)
1. **Manbalar — faqat tekshirilgan + foydalanuvchiniki**: OpenAlex/Crossref (maqola) + **Google Books API** (darslik/monografiya, ISBN; kalit ixtiyoriy, 1 000/kun) + **lex.uz** (qonun/farmon/qaror — `fast` rol qidiruv → URL fetch → sarlavha/raqam/sana tasdig'i; tasdiqlanmasa RAD) + foydalanuvchi ro'yxati (DOI/ISBN/matn). Uydirma manba HECH QACHON; yetmasa hisobot «N ta manba yetishmayapti — o'zingiz qo'shing» + «Sizdan kutiladi». Ro'yxat tartibi — O'zbekiston qoidasi: qonun/farmon/qaror → VM/vazirlik → kitob/monografiya → maqola → statistika → internet («Murojaat sanasi»).
2. **Tezis → Maqola dvigateliga**: `thesis` vositasi menyuda qoladi, ichida `conference_thesis`/`conference_extended` turlari + konferensiya hajmi (1 bet ≈300–450 so'z / 2–3 bet / 3–5 bet); eski IMRAD yo'li o'chiriladi; narx jadvali (4–8 ming) saqlanadi.
3. **Insho — uch kontekst**: maktab/DTM (uz; mulohazali/tavsiflovchi/umumlashtiruvchi/argumentli, adabiy — epigraf + asar iqtibosi), OTM akademik esse (uz/ru/en, 500–1 000 so'z, thesis statement + topic sentence), IELTS Task 2 (en, ≥250 so'z, TR/CC/LR/GRA band 1–9). TOEFL insho (2026 dan bekor) kiritilmaydi.
4. Hisobot (qoidalar + Claude baholovchi) + avto-sayqal (Q-3) + halollik chegarasi (Q-2, `HONESTY_LIMIT`, `needsUserData`) + ko'ruvchida tahrir — beshalasiga. **Narxlar o'zgarmaydi** (tannarx: kurs ishi ≈3–4 ming so'm vs 12–24 ming; referat ≈1.5 ming vs 3–6 ming; insho ≈0.7 ming vs 2–4 ming).
5. «Bezak maydon yo'q» — har forma parametri reyestrda + differensial zond + mutatsiya.

### Standartlar (tadqiqotdan — dvigatel qoidalari)

**Kurs ishi**: titul → mundarija → kirish → boblar → xulosa → adabiyotlar → ilovalar (ixtiyoriy). Titul 9 maydon (vazirlik — 2022 dan «Oliy ta'lim, fan va innovatsiyalar», TATU'da ikkinchi vazirlik ham → tanlov + «o'z matnim»; OTM; fakultet; kafedra; «FAN» fanidan KURS ISHI; mavzu; Bajardi guruh+FISh; Tekshirdi daraja+FISh; shahar–yil). Bob `1-BOB. NOM` (bosh harf), paragraf `1.1.`, har bobda ≥2 paragraf; gumanitar/iqtisodiy 2 bob×2, tabiiy/texnik «adabiyot sharhi → tajriba/hisob → natijalar muhokamasi». Kirishda MAJBURIY 7 element: dolzarblik, maqsad, vazifalar, obyekt, predmet, metodlar, ish tuzilmasi; kirish hajmi 10–15 % (3–5 bet); umumiy 25–35 bet (default 28–30), xulosa 2–4 bet, asosiy ≈50–60 %. TNR 14/1.5, chegara 3/1.5/2/2 (gumanitar o'ng 1.0), xatboshi 1.25, titulda sahifa raqami yo'q. Jadval raqami TEPA O'NGDA `1.1-jadval`, nomi ustida markazda, «Manba:» ostida 10 pt kursiv; rasm `1.1-rasm. Nom` OSTIDA markazda, matnda oldin havola; formula `(1.1)` o'ngda + «bu yerda …»; ilova `1-ILOVA`. Adabiyotlar ≥15 (BuxDU 15–20), bibliografiya GOST 7.1 o'zbek shakli `Muallif. Nomi. – Shahar: Nashriyot, yil. – N b.`, maqola `// Jurnal. – yil. №N. – B. xx-yy.`, iqtibos `[3, 45-b.]`, ro'yxatga faqat matnda havola qilinganlar; iqtibos ulushi ≤10 %, ilova ≤1/3. Turlari: nazariy / amaliy-hisob (tajriba qismi) / kurs loyihasi (chizma, hisob). Fan profillari: texnik (hisob, blok-sxema), tabiiy (IMRAD-ga yaqin), iqtisodiy (statistik jadval, Statistika qo'mitasi, atamalar lug'ati), gumanitar (kengaytirilgan kirish: ilmiy yangilik, farazlar), huquqiy (normativ hujjatlar boshda, lex.uz).
**Referat**: 6 element (titul, reja, kirish, asosiy qism — BO'LIMLAR, xulosa, adabiyotlar; ilova/annotatsiya yo'q); «manba mazmunining qisqa bayoni — matnda qanday yangilik bor?»; turlari informativ / tahliliy-taqqoslovchi / baholovchi / doklad (3 qism, sxema-jadval); hajm 10–15 bet, kirish 1–1.5, xulosa ~1; adabiyotlar ≥5–8 alifbo; baholash: mazmun, mantiq, tahlil, rasmiy talab, ijodiylik.
**Mustaqil ish** (VM 824, OTV 311): titul → mundarija → kirish → asosiy (nazariy + tahliliy + amaliy) → xulosa → adabiyotlar; hajm 5–15 bet (default 10); yozma bo'lmagan shakllar (taqdimot, buklet, test, Venn) — keyingi sprint.
**Insho**: yuqoridagi 3 kontekst; klişe taqiqi («bugungi kunda», «xulosa qilib aytganda» + `FILLER_PHRASES`); kirish/xulosa har biri 12–20 %.
**Tezis**: UDK → sarlavha → muallif/OTM/email → annotatsiya 3–5 qator → kalit so'zlar 3–6 → matn (gap→maqsad→usul→natija→xulosa) → adabiyotlar; hajm konferensiyaga qarab (yagona standart yo'q); TNR 14/1.5, chegara 2.5/2.5/2.5/1.5.

### Arxitektura

**Qaror:** yangi `lib/generation/work/` (kurs ishi/referat/mustaqil ish) va `lib/generation/essay/` dvigatellari; maqola dvigateli UMUMLASHTIRILMAYDI — undan faqat SOF bloklar neytral `lib/generation/report/` qatlamiga ko'tariladi. Sabab: `articleWordPlan` (tekis bo'limlar, titul/mundarija/ilova yo'q) va qat'iy `SkeletonSection[]` (kurs ishida bob soni/nomi mavzudan chiqadi) talaba ishiga yaramaydi; insho manbasiz, hajmi so'zda, mezoni tashqi rubrika.

```
R0 substrat (lead):
  lib/generation/types.ts        Reference (+kind: article|book|law|web|user, isbn, docNo, docDate, accessed), Figure, FigureSpec, FigureKind — article/types.ts dan ko'chadi (re-export qoladi)
  lib/generation/report/         types.ts (DocReview = ArticleReview alias, ReviewCheck, PolishLog, UserNeed), score.ts (scoreReview, visualCoverage, trigrams/jaccard),
                                 judge.ts (judgeSystemPrompt/parseJudge/neutralJudge/JUDGE_*), text.ts (sampleForJudge — PROPORSIONAL kesish: har bo'limdan ulush + kirish/xulosa to'liq),
                                 guard.ts (guardSection, factNumbers), polish-core.ts (needsUserData, HONESTY_LIMIT, applyPolish, runPolish — rewrite/review DEPENDENSIYA, acceptDelta)
  research/pipeline.ts           collectReferences(ask: ResearchAsk{topic,keywords,userRefs,research,language,want,kinds,tiny})

lib/generation/work/   types.ts (WorkGenreId coursework|referat|independent, WorkKindId, SubjectProfileId, WorkModel=AcademicDoc.work, WorkOutline bob→paragraf, WORK_LIMITS)
                       registry.ts (janr×tur: skelet, guidance[], judge{skip,describe}, hajm, refsMin) · subjects.ts (5 fan profili) · input.ts · plan.ts (workWordPlan: titul/mundarija/ilova apparaturasi, kirish 10–15 %, xulosa 2–4 bet)
                       prompts.ts (en tizim prompti + TYPE RULES + SUBJECT RULES + HONESTY LIMIT) · engine.ts (buildWorkDoc) · guard.ts (intakeCheck — kirish 7 element) · review.ts (~16 qoida + 5 judge mezoni) · polish.ts · layout.ts (planWork — YAGONA MANBA) · edit.ts (WorkOp)
lib/generation/essay/  types.ts · registry.ts (kontekst×tur, guidance, judge) · input.ts · prompts.ts · engine.ts · rubric.ts (DTM 24→100 / IELTS band→ball / OTM rubrika — vaznlar BITTA joyda) · layout.ts (design ramkasi saqlanadi)
lib/generation/work-params.ts, essay-params.ts   reyestr + differensial zond
```

`buildWorkDoc` bosqichlari: research 0→10 (lex.uz + Books + OpenAlex/Crossref + user) → outline 10→18 (bob/paragraf, `tocText` bo'lsa undan; kirish elementlari) → sections 18→60 (`mapPool(3)`, har paragraf alohida, `guardSection`) → visuals 60→70 (jadval `1.1-jadval`, sxema `1.1-rasm`, `figures/buildFigures`) → refs 70→76 (`verifyCitations` → citedOnly → `orderUzReferences`) → review 76→86 → polish 86→94 (Q-3 `work` uchun `> +2`) → render 94→100. LLM — `complete("writer"|"judge"|"fast")`, `CostMeter` → `cost_json`.

**Hisobot qoidalari (work)**: introParts (7), introShare (10–15 %), chapterBalance (≥2 paragraf, boblar ±30 %), length, conclusionShare, refsCount (janr minimumi), refsOrder, refsVerified (100 %), refsCited, visualRef, tocMatch, filler, repetition, unsourcedNumbers, userFacts, pageLimit. **Judge**: logic, depth, style, aimMatch (xulosa ↔ vazifalar), originality.

**Render/paritet/tahrir**: `docx-profile.ts workProfile(subjectProfile)` (`articleProfile` naqshi; `profileFor` ga shox), `render-docx.ts drawWork(plan)` (`drawArticle` naqshi), `title-model.ts` (+`ministryCustom`, ikkinchi vazirlik), `lib/viewers/flow.ts` `work` shoxi, `lib/server/edit-adapters.ts workAdapter` (tools: coursework/referat/mustaqil-ish/essay), `lib/server/doc-polish.ts` (adapter id bo'yicha article|work|essay; `article-polish.ts` o'rniga), `ResultView.tsx` review = `doc.article?.review ?? doc.work?.review ?? doc.essay?.review`. Eski hujjatlar: `legacyWorkModel(doc)` faqat o'qish (`legacyArticleModel` naqshi).

**Forma**: `components/forms/WorkComposer.tsx` (`ArticleComposer` naqshi, `custom: "work"` dispatch `ToolWorkspace.tsx`): mavzu, fan, tur (`workKind`), fan profili, titul 9 maydoni (vazirlik tanlov + «o'z matnim»), hajm (mavjud chiplar, narx o'zgarmaydi), mundarija (avto / o'zimniki — ko'rinadigan), «Materiallarim» (fayl + faktlar), o'z manbalari (DOI/ISBN/matn), vizuallar (`includeVisuals`, `figureCount`, `figureKinds`, `tableCount`), `refsMin` (standart kurs 15 / referat 5 / mustaqil 8), til, qo'shimcha. `EssayComposer.tsx`: kontekst × tur, hajm (varaq/so'z), asar/epigraf (adabiy), `design`, til. Qoralama `useFormDraft`.

**Tezis**: `tools.ts thesis` → `custom: "article"`; `ArticleComposer` `tool.id === "thesis"` da faqat `conference_thesis`/`conference_extended` + «Konferensiya hajmi» (`thesisLength` parametri reyestrda; `wordRange` variantlari); `priceFor` tezis shoxi saqlanadi; o'chiriladi: `write-specials.ts writeImradWithLlm`, `write-llm.ts` dispatch, `prompts.ts imradSystemPrompt/thesisSystemPrompt`, `structure.ts` tezis shoxlari + testlari; eski tezis hujjatlari `legacyArticleModel` bilan ochiladi.

**Manbalar (WP-B)**: `research/googlebooks.ts` (`searchBooks`, `industryIdentifiers ISBN_13`, kesh `gb:`), `research/lexuz.ts` (`findLaw` → `fetchLaw` tasdiq; 8 s + 1 retry; tasdiqlanmasa `null`; kesh `lex:` 30 kun), `cite/gost.ts` (+`formatGostBook/Law/Web`), `cite/order.ts orderUzReferences`, `pipeline.ts` kinds bo'yicha tarmoqlar + ISBN dedup.

### Ish paketlari (≤2 agent parallel, `scripts/heavy.sh`, bitta test fayli, `assert.ok(!el)`, agent avval `git merge main`, fayl egaligi qat'iy)

| Raund | WP | Kim | Egalik | Testlar (taxminiy) | Mutatsiya | Kun |
|---|---|---|---|---|---|---|
| R0 | Substrat | lead | `generation/types.ts`, `report/*`, `article/{types,review,polish,guard}.ts` (faqat import/re-export), `research/pipeline.ts` | `report-core` ~12; barcha `article-*` o'zgarishsiz yashil (kommit sharti) | ≥3 | 1 |
| R1 | **WP-A work dvigateli** | agent (opus) | `work/**` (layout/edit dan tashqari), `work-params.ts` | `work-registry` 8, `work-input` 10, `work-plan` 8, `work-engine` 12, `work-review` 16, `work-polish` 8, `work-params` zond | ≥5 | 2.5 |
| R1 | **WP-B manbalar** | agent (opus) | `research/{googlebooks,lexuz,pipeline,cache}.ts`, `cite/{gost,order,index}.ts` | `research-googlebooks` 8, `research-lexuz` 10 (tasdiq radi shart), `research-pipeline` +6, `cite-gost` +8, `cite-order` 8 | ≥4 | 2 |
| R2 | **WP-C render+paritet+tahrir** | agent (opus) | `work/layout.ts`, `work/edit.ts`, `render-docx.ts`, `docx-profile.ts`, `title-model.ts`, `viewers/flow.ts`, `edit-adapters.ts` | `work-layout` 14, `work-docx` 12, `work-edit` 10, `viewer/work-parity` 8, `viewer/work-legacy` 4, `work-commit` 6 | ≥5 | 2 |
| R2 | **WP-D insho** | agent (opus) | `essay/**`, `essay-params.ts` | `essay-registry` 6, `essay-engine` 10, `essay-rubric` 12, `essay-review` 10, `essay-params` zond | ≥4 | 2 |
| R3 | **WP-E formalar** | agent (sonnet) | `WorkComposer.tsx`, `EssayComposer.tsx`, `work-fields.tsx`, `ToolWorkspace.tsx` | `ui/work-composer` 12, `ui/essay-composer` 8, `viewer/work-form` 4 | ≥3 | 1.5 |
| R3 | **WP-F tezis + server** | lead | `tools.ts`, `ArticleComposer.tsx`, `write-llm.ts`, `write-specials.ts`, `prompts.ts`, `structure.ts`, `server/doc-polish.ts`, `api/generations/[id]/{polish,rewrite}`, `ResultView.tsx`, `budget.ts` | `pricing` +4, `thesis-legacy` 4, `doc-polish-route` 6, `ui/work-polish` 6 | ≥3 | 1.5 |
| R | Integratsiya | lead | `live-engine.mts`, `docs/AUDIT-19.md`, `.claude/structure.md`, `CLAUDE.md`, `docker-compose.yml`, `compose-env` test, README | jonli + smoke + ko'z + deploy | — | 1.5 |

Jami ≈ 8–9 kun ish, kalendar ~5–6 kun.

### Tekshirish

- **Unit/mutatsiya**: har WP bittadan fayl, `scripts/heavy.sh`; mutatsiyalar: bob balansi, kirish 7 element, hajm ulushi, tur farqi, manba minimum, lex tasdiq, ISBN dedup, tartib guruhi, kalitsiz Books, jadval raqami joyi, rasm ostida, paritet, IELTS band→ball, klişe, thesis statement, ministry «o'z matnim», tezis narxi, adapter tanlovi, Q-3 `+2`.
- **Jonli** (`npm run live -- …`): `coursework-theory` (gumanitar 25–30, tocMethod=ai), `coursework-applied` (texnik 35–40, userFacts + hisob bobi, blok-sxema), `referat` (informativ 10–15), `referat-analytic`, `independent` (10), `essay-dtm` (uz, adabiy), `essay-academic` (en 800 so'z), `essay-ielts`, `thesis` (1 bet). Har birida: manbalar 100 % tekshirilgan, `unresolved` bo'sh, tartib to'g'ri, kirish 7 elementi, `1.1-jadval`/`1.1-rasm`, hisobot > 0, `cost.calls > 0`, sahifa paket ichida; tannarx `cost-report`.
- **Chromium smoke** (`pw/work19.mjs`): kurs ishi forma → natija (hisobot, «Hammasini tuzatish», «Sizdan kutiladi», tahrir → saqlash → DOCX), insho × 1, tezis × 1; 0 brauzer xatosi.
- **LibreOffice ko'z**: titul 9 maydon, mundarija raqamlari, `1-BOB`, `1.1-jadval` tepa o'ngda + «Manba:», `1.1-rasm` ostida, `(1.1)`, titulda raqam yo'q, adabiyot tartibi, ilova; insho ramkasi; tezis chegarasi.
- **Deploy**: migratsiya kerak emas (`Reference.kind` doc_json ichida, `source_cache` prefiks); env `GOOGLE_BOOKS_API_KEY` (ixtiyoriy) — `.env.example` + compose web/worker + `compose-env` test; `budget.ts WORK_MS` (45 bet ≈ 480 s + sayqal) 660 s ichida; zaxira → nohup deploy → prod smoke (eski kurs ishi/referat/tezis ochilishi + yangi hujjat).

### Xavflar
X-1 Google Books'da o'zbek darsligi kam → kvota mo'ljal, yetmasa `UserNeed{refs}` (uydirma yo'q); X-2 lex.uz bloklanishi → tasdiqsiz manba qo'shilmaydi, kesh; X-3 45 betlik byudjet → `mapPool(3)`, 40–45 betda avto-sayqal o'tkazib yuboriladi (qo'lda tugma qoladi); X-4 baholovchi 25k belgi kesimi → `sampleForJudge` proporsional (maqola ham shunga o'tadi); X-5 baholovchi shovqini → `acceptDelta +2`; X-6 eski hujjatlar → legacy o'qish; X-7 `polish-core` refaktori → R0 faqat ko'chirish, article testlari o'zgarishsiz yashil; X-8 DTM mezon taqsimoti taxminiy → panelda izoh, vaznlar bitta joyda.


## 2. Tadqiqot manbalari
- Kod xaritasi (Explore agent, opus): 5 vosita eski avlod yo'lda — `writeWriterWithLlm`/`writeEssayWithLlm`/`writeImradWithLlm`, uydirma manbalar, hisobot/sayqal/tahrir/sxema yo'q, janr farqi 4 ta `if`, jonli holat faqat essay+coursework, forma zondsiz.
- Standartlar (Explore agent, opus): BuxDU «Lazer fizikasi» kurs ishi uslubiy ko'rsatmasi, TATU Qarshi filiali individual loyiha qo'llanmasi, TSUULL «Ilmiy tadqiqot faoliyati asoslari», TDIU iqtisodiy nazariya kurs ishi ko'rsatmasi, Nordic Univ. magistrlik qo'llanmasi, ГОСТ 7.32-2017, VM 824 (31.12.2020) + OTV 311 (16.07.2021), infoedu.uz/mustaqilish.uz, antiplag.uz/aslmatn.uz.
- Insho/tezis (Explore agent, sonnet): bilimmarkazi.uz (Milliy sertifikat esse turlari), IELTS band descriptors, TOEFL 2026 o'zgarishi (insho bekor), conference.ubtuit.uz / uniwork.buxdu.uz tezis talablari, PRISMA/structured abstract.
- Mahalliy amaliyot: `docs/research/konf-2025-uzb.md` (AUDIT-18).

## 5. Bajarilish yozuvi

- `50f639f` **R0-A (opus agent)** — neytral `lib/generation/report/` qatlami: maqolaning hisobot/sayqal mantig'i KO'CHIRILDI (X-7 — `article/*` xulqi o'zgarmadi, faqat re-export va yupqa o'ramlar). `types.ts` (`DocReview` = `ArticleReview`, `UserNeed.id: string`, generik `JudgeResult<C>`/`JudgeSpec<C>` — mezonlar spetsifikatsiyadan: maqola 6, kurs ishi 5, IELTS 4), `score.ts` (`scoreReviewFor`, `trigrams`/`jaccard`, sof `visualCoverageOf`), `judge.ts` (`judgeSystemPromptFor`/`parseJudgeFor`/`neutralJudgeFor`/`judgeChecksFor`, `roleLine`/`typeNoun`), `text.ts`, `guard.ts`+`filler.ts`, `polish-core.ts` (Q-2, Q-3, `applyPolishWith`/`runPolishWith<Op,J>` — rewrite/apply/review/judgeFromReview/rescore dependensiya, `acceptDelta` X-5). **X-4 yopildi**: `sampleForJudge` — kirish va xulosa to'liq qoladi (chekkalar sig'masa proporsionalga qaytadi). `tests/report-core` 16, 4 mutatsiya; 11 regressiya to'plami o'zgarishsiz yashil. Ochiq: `report/guard.ts` `Reference` ni hali `article/types` dan oladi (WP-B ko'chiradi); `ArticleReviewPanel NEED_HREF` yangi `refs`/`work` bandlari uchun kengaytirilishi kerak (WP-E).
- `374ed05` **WP-F 1/2 (lead)** — «Tezis» maqola dvigateliga (qaror 2): `tools.ts` thesis `custom:"article"` (fields yo'q), `THESIS_TYPE_IDS`/`THESIS_PRICES` (1–2 bet 4 000, 3–5 bet 5 000 — eski 5–10…20–25 «tezis» paketlari tezis emas edi, shu hajm — «Maqola»), `thesisTypeId` (ruxsatsiz tur → `conference_thesis`; narx, dvigatel va tuzilma darvozasi bir xil qoida bilan), `defaultPages("thesis") = "1-2"`; `write-llm.ts` thesis → `buildArticleDoc`, IMRAD yo'li (`writeImradWithLlm`, `imradSystemPrompt`) o'chirildi; `structure.ts` tezis = konferensiya turining majburiy bloklari (`abstract`, `section:body`); `index.ts isArticleTool` (hajm/sahifa darvozalari), `docx-profile` thesis (yangi → nashr profili, eski → GOST titul), `viewerKind` thesis → article (hisobot paneli, tahrir); `ArticleComposer` tezis rejimi (`allowed` turlar, «Tezis turini tanlang», o'z narx yorlig'i, qoralama `thesis` kaliti), `ArticleTypeGallery allowed/title`. Testlar: pricing (+tezis jadvali, mutatsiya «ruxsatsiz tur maqola narxiga o'tsa 12 000»), generation (structureNeeds, standart hajm 2 bet), document (IMRAD testi olib tashlandi — annotatsiya darvozasi `article-engine` da), ui/article-composer tezis rejimi (2 karta, 4 000/5 000, submit `slug: thesis`); seed-demo tezis namunasi; live-engine `thesis` holati (imrad_oak so'ralganda ham conference_thesis, 4 mezon). Chetlanish: tezis narx jadvali 4–8 ming → 4–5 ming (yuqori paketlar tezis emas edi).
- **WP-B + R0-B (opus agent, 6 kommit)** — Manbalar (tekshirilgan, uydirmasiz). `Reference`/`Figure` tiplari `lib/generation/types.ts` ga ko'chdi (`article/types.ts` re-export), `Reference` += `kind`/`isbn`/`docNo`/`docDate`/`issuer`/`accessed`/`pageCount`, `kindOf()` eski hujjatlar uchun turni maydonlardan chiqaradi. Ikki yangi tarmoq: **Google Books** (`research/googlebooks.ts` — `searchBooks`/`verifyIsbn`, ISBN_13 ustun, kesh `gb:`) va **lex.uz** (`research/lexuz.ts` — `fast` rol nomzod beradi, har havola fetch qilinib sahifaning o'zi raqam/sana/sarlavhaning ≥60 % ini tasdiqlashi shart; tasdiqlanmasa RAD, 404 ham RAD, 403/timeout → `blocked`, kesh `lex:` 30 kun). `pipeline.ts collectReferencesFor(ask)` — `kinds` tarmoqlarni tanlaydi, `quota` tanlash promptiga mo'ljal, dedup id/DOI/ISBN/sarlavha; maqola o'rami `collectReferences` o'zgarmadi; `ResearchStats` += `books`/`laws`/`rejected`/`blocked`/`byKind`. `cite/gost.ts` kitob/qonun/internet shakllari («Murojaat sanasi»), `cite/order.ts orderUzReferences` — O'zbekiston tartibi (qonun → Prezident → VM → vazirlik → kitob → maqola → statistika → internet, guruh ichida translit bilan alifbo). 44 test, 15 mutatsiya. Jonli: lex.uz haqiqiy hujjatni tasdiqladi (O'RQ-637), uydirma havola 404 bilan rad etildi; **Google Books kalitsiz 429 — prodda `GOOGLE_BOOKS_API_KEY` majburiy** (bepul, Google Cloud; `.env.example` + compose + `compose-env`). Ochiq: grounding yo'q (`complete()` da opsiya yo'q) — lex.uz oddiy JSON chaqiruv; prod IP dan lex.uz bloklanishi mumkin — deploy smoke'da `stats.blocked`.
- `a59c2de` **WP-F 2/2 (lead)** — tezis jonli sinovi (`npm run live -- thesis`): (1) `extractMeta` maqola/tezis paketini TURGA moslaydi (`normalizeArticlePages` — tezisga «3–5» so'ralsa `targetPages` 2, narx bilan bir xil qoida; ilgari 4 edi); (2) avto-sayqal «cheklovlar qo'sh» tuzatishi tezisni 152 so'zga qisqartirib, ball 78 → 79 oshgani uchun qabul bo'lgan edi — `article/polish.ts rewriteSection` so'z oralig'i (±10/15 %) tashqarisidagi qayta yozuvni RAD etadi (mutatsiya tekshirildi). Qayta jonli: 197 so'z, 2 bet, 73 ball, 11 chaqiruv, `imrad_oak` so'ralganda ham `conference_thesis`.
- **WP-D (opus agent, 4 kommit)** — **insho dvigateli** `lib/generation/essay/`: uch kontekst × besh tur reyestri (`registry.ts` — hajm, tuzilma, `guidance`, kontekst `JudgeSpec`), `rubric.ts` da VAZNLAR BITTA JOYDA (DTM 24 = 8/5/5/4/2 → 100, akademik 100 = 25/25/20/20/10, IELTS band = 4 + ball×5/3 va `mean(band)×100/9`; maktab/IELTS da baholovchi ulushi 0.6, akademikda qoidalar 0.6). **X-8 yopildi**: DTM taqsimoti taxminiy ekani `DTM_NOTE` bilan panelga. `buildEssayDoc`: reja → matn (bitta chaqiruv; `words.max > 1 200` bo'lsa ikki qism) → `guardSection` (klişe/manbasiz raqam, iqtibos id lari o'chadi, hajm chiqib ketsa bir qayta so'rov) → hisobot → avto-sayqal (`runPolishWith`, `acceptDelta: 1`). Matn `sections` da bitta `essay` bo'limi — render/ko'ruvchi o'zgarmadi; narx o'zgarmadi. `reviewEssay` — 12 qoida (hajm, bandlar + kirish/xulosa 12–20 %, thesis statement, topic sentence, klişe, takror, manbasiz statistika, shaxs, epigraf, asar iqtibosi, IELTS bog'lovchilari, sarlavha) + kontekst baholovchisi (IELTS `judge:tr` «Band 7 · 2/3»). `essay-params.ts` — 12 parametr, zond «bezak maydon»ni ushladi (`person` endi modelda, hisobotga ta'sir qiladi). 58 test, 6 mutatsiya. Ochiq (ulash — WP-E1): `write-llm.ts` essay shoxi, `tools.ts` formasi (`EssayComposer`), `edit-adapters`/`doc-polish` essay adapteri, `ResultView doc.essay.review`, `index.ts` hajm darvozasi so'z bilan o'lchanadigan kontekstlar (IELTS 1 bet).
- **WP-A (opus agent, 4 kommit)** — Talaba ishi dvigateli `lib/generation/work/` (kurs ishi / referat / mustaqil ish): `types.ts` (3 janr × 8 tur, 5 fan profili, `WorkModel` = `AcademicDoc.work` — titul 9 maydon + vazirlik `oliy|maktab|custom`, bob→paragraf daraxti, `intro.parts`, `refsMin`, `polishAccept`), `registry.ts` (janr × tur skeleti: kirish elementlari **kurs ishi 7 / referat 3 / mustaqil 4**, bob/paragraf chegaralari, shakl `theory-2x2 | review-experiment-results | theory-analysis-practice | sections` — referat BO'LIMLAR bilan, kirish 10–15 %, xulosa 2–4 bet (referat ≈1), `refsMin` 15/5–6/8, hajm chiplari `lib/tools.ts` bilan aynan, `guidance` en 3–5 qator, `JudgeSpec` — logic/depth/style/aimMatch/originality, ta'riflar turga moslangan), `subjects.ts` (5 profil: `guidance`, `research.kinds`/`quota` — huquqiy law 5, iqtisodiy law 2 + book 4, texnik book 4 + article 6; `rightMarginCm` gumanitar 1,0; `defaultVisuals`), `labels.ts` (uz/ru/en: «1-BOB.» ARAB raqami — `i18n` dagi rim emas, «1.1-jadval», «1.1-rasm», «1-ILOVA»), `input.ts` (`workInputFromValues`/`encodeWorkValues`, O'Z reja parseri — `parseManualOutline` «1-BOB.» sarlavhasini buzardi), `plan.ts` (apparatura: titul 1 + mundarija 1 + adabiyot 0,05×N + sxema 0,45 + jadval 0,25 + ilova 1; matn poli 40 %; `workVisualNumbers` bob bo'yicha), `prompts.ts` (TYPE RULES + SUBJECT RULES + 6 qoida; kirish 7 elementi ALOHIDA so'raladi; xulosa VAZIFALARGA javob; `lengthLine`/`formatRefLine`/`FIGURE_KIND_HELP` maqoladan), `guard.ts` (`intakeCheck` uz/ru/en regex + model `parts`; `chapterBalance` ≥2 paragraf va ±30 %), `engine.ts` (`buildWorkDoc`: research → outline (`tocText` USTUN) → intro (yo'q element 1 marta qayta so'raladi) → paragraphs `mapPool(3)` → visuals → conclusion → refs (`verifyCitations` → `citedOnly` → `orderUzReferences`) → review → polish), `review.ts` (16 qoida + turning baholovchisi), `polish.ts` (`runWorkPolish` = `runPolishWith` o'rami, **Q-3 + X-5: acceptDelta +2**, Q-2 `needsUserData` filtri faktlar bo'lsa ham, «Sizdan kutiladi» — manbalar/materiallar/titul), `work-params.ts` (27 parametr + `WORK_JSON_FIELDS`). `types.ts` ga bitta qator (`work?: WorkModel`), `budget.ts` ga `workBudgetMs(pages)` = 150 000 + 9 000×bet + 90 000 (45 bet ≈ 645 s ≤ 660 s). Matn `AcademicDoc.sections` da TEKIS qoladi (bob = `ch1` sarlavha bo'limi, paragraf = `ch1.1`) — `paginate`/`guardSection`/`verifyCitations`/tahrir oplari o'zgarmaydi. **89 test** (registry 9, input 11, plan 10, engine 22, review 19, polish 15, params zondi 3), **11 mutatsiya** (kirish 7→6, bob balansi, kirish ulushi, refsVerified 100 %, tur farqi, `acceptDelta`, `tocText`, Q-2 filtri, kirish qayta so'rovi, 40–45 bet sayqal skip, vizual raqami). Topilma: Q-3 testi dastlab `WORK_ACCEPT_DELTA` ning O'ZIDAN hisoblagani uchun mutatsiyani o'tkazib yubordi — endi xulq qulflangan (`after − 2` RAD, `after − 3` QABUL). Ochiq: `work/layout.ts`/`edit.ts` (WP-C) kelguncha sayqal `applyWorkSectionOps` (faqat `setSection`) bilan ishlaydi va hisobot raqamlarni `work/plan.ts workVisualNumbers` dan oladi — `planWork` shu hisobni YAGONA MANBA sifatida qayta e'lon qilishi kerak; `figureSpecFromLlm` `article/engine.ts` dan DINAMIK import (u `figures/` → `sharp` ni statik tortadi).
- **WP-E1 (opus agent, 6 kommit)** — **insho mahsulotga ULANDI**. `write-llm.ts` essay shoxi → `buildEssayDoc` (`onCost` → `cost_json`); eski yo'l (`writeEssayWithLlm`, `writeEssayInChunks`, `prompts.ts essaySystemPrompt`) o'chirildi. `index.ts`: hajm darvozasi insho uchun SO'Z bilan (`doc.essay.words.min × 0.9`), sahifa darvozasi `essay.words` bo'lsa o'tkazib yuboriladi (IELTS 1 bet) — `essayGateWords`, `pageGateApplies`. `lib/server/doc-polish.ts` — `article-polish.ts` umumlashtirildi: server qismi hujjat turidan mustaqil, farqi `POLISHERS` jadvalida; **`toOps`** qadam (sayqal hujjat tilida `setEssay`, adapter `setSection`); `essayAdapter` (`ESSAY_OPS`: matn + `setSection` + `review`; `review` opi `doc.article ?? doc.essay`); `rewrite` inshoda 422 (bandma-band tuzatish baholovchisiz nusxa bo'lardi). `EssayComposer.tsx` — kontekst × tur; normallashtirish `toValues → essayInputFromValues → uiFromValues` (server funksiyasi); narx o'zgarmadi (akademik `pages = ceil(so'z/250)`, IELTS 1 varaq). `ResultView` hisobot `doc.article?.review ?? doc.essay?.review`; inshoda «Manbalar»/«Vizuallar» guruhlari yashirin; `NEED_HREF` `/uz/essay#…`; tahrir `useArticleEdit` + `ArtifactViewer case "essay"`. 29 test, 8 mutatsiya; `live-engine` `essay-dtm`/`essay-academic`/`essay-ielts`. Ochiq: jonli, Chromium smoke, LibreOffice ko'z — lead (R).
- **R (lead) — ulash**: `write-llm.ts` kurs ishi/referat/mustaqil ish → `buildWorkDoc` (`WORK_ENGINE=0` — eski yozuvchi yo'liga favqulodda qaytish), `budget.ts budgetFor` tezis → maqola formulasi, talaba ishi → `workBudgetMs`; `assets.ts` `doc.work.figures` aktivga, `preview.ts` talaba ishi eskizi (mavzu + kirish + birinchi sxema); `ResultView` hisobot `doc.article ?? doc.essay ?? doc.work`, `ArticleReviewPanel hrefBase` («Sizdan kutiladi» havolalari vosita formasiga: `/uz/coursework#userRefs`), `doc-polish.ts POLISHERS.work` (`runWorkPolish`, `setSection` — adapter WP-C). **Insho jonli** (`essay-dtm`/`essay-academic`/`essay-ielts`): 3/3 — DTM adabiy 638 so'z/3 bet/79 ball (epigraf, «O'tkan kunlar»), akademik 775 so'z/87 ball (thesis statement, 6 topic sentence, 3-shaxs), IELTS 274 so'z/94 ball (4 band, «uz» so'ralsa ham en, 1 bet qabul); tannarx 3–6 chaqiruv.
- **WP-E2 (agent, 6 kommit)** — kurs ishi/referat/mustaqil ish uchun UMUMIY forma `components/forms/WorkComposer.tsx` (`ArticleComposer` naqshi): mavzu+tur+fan profili, `<details open>` titul (9 maydon + vazirlik `oliy|maktab|custom`, «o'zim yozaman» matni), hajm/til, reja (`ai|manual` + jonli «N bob, M paragraf» hisobi — `work/input.ts parseWorkOutline` bitta manba), materiallar (fayl, natijalar, manbalar DOI|ISBN|matn, `refsMin`), yig'iladigan sozlamalar (vizuallar, sxema turlari — `ArticleComposer FigureKindChips` eksport qilib qayta ishlatildi, jadval soni). `lib/tools.ts`: coursework/referat/mustaqil-ish `fields: []` + `custom: "work"`ga o'tdi, `CUSTOM_REQUIRED.work` (universitet + muallif) — narx jadvali O'ZGARMADI. **45 test** (`tests/ui/work-composer.test.mts` 14, `tests/viewer/work-form.test.mts` 5, qolgani regressiya — article-composer/article-form/pricing/validate/work-input/work-registry/generation/viewer-kind), **4 mutatsiya** (vazirlik/reja ko'rinish bog'lanishi, `includeVisuals→disabled`, `CUSTOM_REQUIRED.work` universitet, qamrov `data-field`). Topilma: `ToolWorkspace`ni SSR (`renderToStaticMarkup`) bilan sinab bo'lmaydi — zustand v5 `useSyncExternalStore` server snapshot'i `getInitialState()`dan o'qiydi, `setState` render oldidan chaqirilsa ham ko'rinmaydi; haqiqiy dispatch jsdom (`tests/ui/`)da tekshirildi. Mening o'zgarishim (`tool.fields: []`) 3 ta MAVJUD testni buzdi — barchasi tuzatildi (`work-registry` narx jadvali bilan, `generation` standart hajm ALOHIDA blok bilan, `viewer-kind` `custom !== "work"` istisnosi bilan); yo'lda `tests/viewer/article-review-panel.test.mts` dagi menga aloqasi yo'q, oldindan buzuq (WP-F, `374ed05`) brittle regex ham tuzatildi. Ochiq: WP-C (`work/layout.ts`/render-docx/ko'ruvchi) hali ulanmagan — forma tayyor, lekin «Yaratish» bosilganda dvigatel `buildWorkDoc`ga borishini keyingi WP tasdiqlashi kerak (jonli sinov `npm run live -- coursework` shu WP qatorida).
